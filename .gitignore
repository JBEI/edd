# Various IDE and OS files to ignore
.DS_Store
*.pyc
*.swp
.pydevproject
.idea
tscommand-*
.tscache

# per-machine configuration file; contains secrets, DO NOT CHECK-IN
secrets.env

# per-machine local settings file; this will override default settigns with local changes
edd/settings/local.py
docker-compose.override.yml
# also ignore any local media/upload folders
/media
/upload
/uploads
# and ignore local log directory
/log
# and ignore the "development" static folder
/static

# ignore typescript generated files in mounted code folders
main/static/main/js

# these no longer should get generated, but older clones may have these files still
secrets.cfg
bower_components
node_modules
